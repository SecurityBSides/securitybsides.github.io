import React from "react";
import { Cal, Layout } from "../components";

const CalPage = () => {
   return (
      <Layout>
	   <h1>Direct reference to manual calendar</h1>
         <Cal />
      </Layout>
   );
};

export default CalPage;
